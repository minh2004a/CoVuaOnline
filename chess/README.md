# Chess Frontend (Vite + React + Tailwind)

## Scripts
- `npm run dev`: start local dev server
- `npm run build`: production build
- `npm run preview`: preview production build

## Legacy Bridge
- Original HTML/CSS/JS is preserved at `public/legacy/*`
- React app loads legacy shell + scripts to keep existing chess logic unchanged